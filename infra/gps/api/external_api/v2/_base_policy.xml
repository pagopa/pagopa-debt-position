<policies>
  <inbound>
    <set-variable name="requestPath" value="@(context.Request.Url.Path)" />
    <set-variable name="sub" value="@(context.Subscription.Name)" />
    <set-variable name="subid" value="@(context.Subscription.Id)" />
    <set-variable name="subkey" value="@(context.Subscription.Key)" />
    <base />
    <!-- rate limit by subscription key -->
    <choose>
      <when condition="@(!((string)context.Request.Headers.GetValueOrDefault("X-Orginal-Host-For","")).Equals("api.prf.platform.pagopa.it") && !((string)context.Request.OriginalUrl.ToUri().Host).Equals("api.prf.platform.pagopa.it"))">
        <!-- livellamento: evita che le 1000 chiamate della finestra si concentrino in un solo secondo -->
        <rate-limit-by-key calls="100" renewal-period="1" counter-key="@(context.Subscription.Key + "|burst")" increment-condition="@(context.Response.StatusCode != 429)" remaining-calls-header-name="x-remaining-calls-per-second" />
        <rate-limit-by-key calls="1000" renewal-period="10" counter-key="@(context.Subscription.Key + "|window")" increment-condition="@(context.Response.StatusCode != 429)" remaining-calls-header-name="x-remaining-calls-per-period" />
        <choose>
          <!-- la lista paginata delle PD e' la piu' costosa: tetto aggiuntivo piu' restrittivo. Il match sul path invece che su context.Operation.Id evita dipendenze dalla normalizzazione degli operationId fatta da APIM in fase di import OpenAPI, ed esclude naturalmente /debtpositions/{iupd} e i sotto-percorsi publish/invalidate -->
          <when condition="@(context.Request.Method == "GET" && context.Request.Url.Path.TrimEnd('/').EndsWith("/debtpositions", StringComparison.OrdinalIgnoreCase))">
            <rate-limit-by-key calls="45" renewal-period="1" counter-key="@(context.Subscription.Key + "|list-burst")" increment-condition="@(context.Response.StatusCode != 429)" remaining-calls-header-name="x-remaining-calls-per-second" />
            <rate-limit-by-key calls="450" renewal-period="10" counter-key="@(context.Subscription.Key + "|list-window")" increment-condition="@(context.Response.StatusCode != 429)" remaining-calls-header-name="x-remaining-calls-per-period" />
          </when>
        </choose>
      </when>
    </choose>
    <!-- Calling Authorizer's fragment -->
    <set-variable name="application_domain" value="gpd" />
    <set-variable name="metadata" value="no-metadata" />
    <set-variable name="debtpositionsRequestPath" value="@("/pagopa-gpd-core/organizations/" + (string)context.Request.MatchedParameters["organizationfiscalcode"] + "/debtpositions")" />
    <choose>
        <when condition="@(((string)context.Variables["requestPath"]).Equals((string)context.Variables["debtpositionsRequestPath"]) && ((string)context.Request.Method).Equals("POST"))">
            <rewrite-uri template="@("/organizations/" + (string)context.Request.MatchedParameters["organizationfiscalcode"] + "/debtpositions/bulk")" copy-unmatched-params="true" />
        </when>
    </choose>
    <choose>
      <!-- Making sure that will excludes all APIs that does not includes CI fiscal code -->
      <when condition="@(context.Request.MatchedParameters.ContainsKey("organizationfiscalcode") && !context.Request.Headers.GetValueOrDefault("Ocp-Apim-Subscription-Key", "").Equals(""))">
        <set-variable name="authorization_entity" value="@(context.Request.MatchedParameters["organizationfiscalcode"])" />
        <include-fragment fragment-id="authorizer" />
        <include-fragment fragment-id="segregation-codes-gpd"/>
      </when>
    </choose>
  </inbound>
  <outbound>
    <base />
  </outbound>
  <backend>
    <base />
  </backend>
  <on-error>
    <base />
  </on-error>
</policies>