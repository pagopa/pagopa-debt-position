name: Create Dashboard

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment for which create dashboards and alerts'
        required: true
        type: choice
        options:
          - prod

permissions:
  id-token: write
  contents: read

jobs:
  dashboard:
    runs-on: ubuntu-22.04

    strategy:
      matrix:
        environment: [ prod ]
        product: [ internal-v1, internal-v3, v1, v3 ]
      max-parallel: 1
    environment:
      name: ${{ inputs.environment }}

    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@1f9a0c22da41e6ebfa534300ef656657ea2c6707
        with:
          persist-credentials: false

      - name: Generate dashboard package
        run: |
          python -m venv venv
          source venv/bin/activate
          git clone --branch fix-availability-dashboard https://github.com/pagopa/opex-dashboard
          cd opex-dashboard
          git checkout 6c6b91d31133081d4b954e6be0a8f941e3559733
          pip install -e .
          cd ..
          source venv/bin/activate
          venv/bin/opex_dashboard generate \
            --template-name azure-dashboard \
            --package ./dashboard \
            --config-file .opex/gpd/${{ matrix.product }}/env/${{ matrix.environment }}/config.yaml

      - name: Copy Environments
        shell: bash
        run: |
          cp -R .opex/gpd/${{ matrix.product }}/env ./dashboard/azure-dashboard

      - name: Setup Terraform
        id: setup_terraform
        uses: hashicorp/setup-terraform@8feba2b913ea459066180f9cb177f58a881cf146
        with:
          terraform_version: 1.9.8

      - name: Login
        id: login
        uses: azure/login@92a5484dfaf04ca78a94597f4f19fea633851fa2
        with:
          client-id: ${{ secrets.CLIENT_ID }}
          tenant-id: ${{ secrets.TENANT_ID }}
          subscription-id: ${{ secrets.SUBSCRIPTION_ID }}

      - name: Terraform Apply
        shell: bash
        env:
          ARM_CLIENT_ID: ${{ secrets.CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.SUBSCRIPTION_ID }}
          ARM_USE_OIDC: true
        run: |
          cd ./dashboard/azure-dashboard
          
          bash ./terraform.sh apply ${{ matrix.environment }} -auto-approve